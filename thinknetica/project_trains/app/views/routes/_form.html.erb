<%= form_for(route) do |f| %>
    <%= render 'common/errors', resource: @route %>

    <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
    </div>

    <%= render 'common/button', resource: @route %>
<% end %>
